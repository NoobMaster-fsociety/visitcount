import React from "react";
import ReactDOM from "react-dom";
import VisitorCounter from "./App";

ReactDOM.render(
  <React.StrictMode>
    <VisitorCounter />
  </React.StrictMode>,
  document.getElementById("root")
);
